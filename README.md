MyApplication

This repository contains an Android app using Jetpack Compose and Firebase authentication.

Notes:
- `google-services.json` is currently included in the `app/` folder per owner request. Remove and rotate keys before publishing.
- Branches: feature branches for refactors and security hardening.

